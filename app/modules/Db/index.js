import React from 'react';
import { browserHistory } from 'react-router';

class db extends React.PureComponent {
  render() {
    return (
        <div>
          db
        </div>
    );
  }
}

export default db;
